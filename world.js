export class World {
	constructor(canvas, ctx) {
		this.Delta = 6;
		this.delta = 2.0;
		this.Lx = canvas.width / this.delta;
		this.Ly = canvas.height / this.delta;

		this.
	}
}

let isDrawing = false;

function update(ctx) {
}
